# frozen_string_literal: true

RSpec.describe RubyWin::Zip::Ffi do
  it "has a version number" do
    expect(RubyWin::Zip::Ffi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
